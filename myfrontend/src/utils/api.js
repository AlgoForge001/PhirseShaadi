// API utility functions will go here
// TODO [BACKEND]: Set base URL
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:5000/api',
  headers: {
    'Content-Type': 'application/json'
  }
})

export default api
